/**
 * 본문 스크롤 영역을 가리키는 표식.
 *
 * 시트는 창이 아니라 자기가 스크롤한다. 목차는 그 요소의 자식이 아니라
 * 형제라서 참조를 물려받을 길이 없어, 이 표식으로 찾는다.
 *
 * `"use client"` 파일에 두면 안 된다 — 이 값을 붙이는 쪽(TheoryView)은 서버
 * 컴포넌트라, 클라이언트 모듈에서 가져오면 문자열이 아니라 클라이언트 참조가
 * 넘어와 어트리뷰트 이름이 함수가 되어 버린다.
 */
export const SHEET_SCROLL_ATTR = "data-sheet-scroll";
